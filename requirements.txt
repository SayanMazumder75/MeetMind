# AI Meeting Notes — Requirements (Lightweight Cloud Edition)
# Install with: pip install -r requirements.txt
#
# Total install size: ~50 MB (vs ~4 GB in the original version)
# No GPU, no CUDA, no local model downloads needed.

# ── Audio capture ──────────────────────────────────────────────────────────────
pyaudio>=0.2.13
sounddevice>=0.4.6
numpy>=1.24.0

# ── Speech recognition (AssemblyAI cloud) ─────────────────────────────────────
# Supports Hindi, Bengali, and 99+ languages. No local model needed.
assemblyai>=0.18.0

# ── Summarization (Claude API) ────────────────────────────────────────────────
anthropic>=0.25.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.28.0
streamlit-autorefresh>=1.0.1

# ── Export ────────────────────────────────────────────────────────────────────
fpdf2>=2.7.6
python-docx>=1.1.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-api-python-client>=2.100.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.4.0
rich>=13.6.0

# ── Optional: local Whisper fallback (only if you can't use AssemblyAI) ───────
# Uncomment below — adds ~200 MB and requires torch
# openai-whisper>=20231117
# torch>=2.0.0

# ── Optional: heavy local summarizer (NOT recommended — 1.5 GB download) ──────
# transformers>=4.35.0
# spacy>=3.7.0
# sentencepiece>=0.1.99